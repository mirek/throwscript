{
  "name": "throwscript-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b packages/core packages/cli",
    "lint": "oxlint .",
    "test": "pnpm run build && pnpm -r run test",
    "check": "pnpm run build && pnpm run lint && pnpm run test"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "oxlint": "^1.72.0",
    "typescript": "^5.9.3"
  }
}

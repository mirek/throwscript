{
  "name": "throwscript",
  "version": "0.2.0",
  "description": "Linter-like checker: every function that can throw must document it with JSDoc @throws",
  "type": "module",
  "license": "MIT",
  "bin": {
    "throwscript": "./dist/main.js"
  },
  "main": "./dist/main.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@throwscript/core": "workspace:*",
    "typescript": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0"
  }
}
